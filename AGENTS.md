# ShotSort Agents

이 파일이 프로젝트 전역 규칙이다. 이슈를 plan / code / tdd / DevLoop / fast-track / 서브에이전트로 다룰 때 먼저 읽는다.

## 디자인 스킬 — 모든 이슈

모든 이슈에서 디자인 스킬을 사용할 수 있고, UI가 있는 작업에서는 사용해야 한다. 호출을 기다리지 않는다.

- **있으면 `ui-design`, 없으면 공식 `frontend-design`.** 둘 다 없으면 기존 디자인을 유지하고, 필요할 때만 새 디자인을 추천한다.
- `ui-design`이 있으면 그 파일의 `explicit-only`는 이 저장소에서 적용하지 않는다.

### 어떤 스킬인가

있는 것 중 **첫 번째**를 읽고 그대로 따른다.

1. `~/.agents/skills/ui-design/SKILL.md`
2. `~/.agent-skills/ui-design/SKILL.md`
3. 설치된 공식 `frontend-design`

공유 디자인 시스템은 없다. 스킬 Step 0이 기존 랜딩·GUI를 찾으면 match, 없으면 `references/new-design.md`. 사용자가 새 룩을 요청하면 new-design.

### 이슈마다

1. 이슈 본문·플랜을 읽은 뒤, 위에서 고른 스킬 파일을 **구현 전에** 읽는다. 둘 다 없으면 기존 디자인을 유지하고, 필요할 때만 새 디자인을 추천한다.
2. 랜딩(`docs/landing/`), GUI(`app.py`), 카피, CTA, 빈/로딩/에러 상태, 키보드, 반응형 중 하나라도 이슈 범위면 스킬을 적용한다.
3. 엔진·CLI·서명만이고 사용자 화면이 없으면, 스킬을 찾았을 때만 “UI 없음, 적용 스킵”을 한 줄로 남긴다. 나중에 UI가 붙으면 그때 적용한다.
4. tdd / 서브에이전트 프롬프트에 이 절을 넣는다.
5. 감사·이슈 초안만이면 코드를 건드리지 않는다. 구현 요청이면 스킬의 제품·접근성 체크를 통과할 때까지 고친다.

### 이 제품에 적용할 때

ShotSort는 macOS 스크린샷 정리 앱이다. 기본 사용자는 비개발자이고, 화면 속 텍스트는 기본값으로 Mac 안에 머문다. 한국어 UI.

- 기존 디자인 소스: `docs/landing/styles.css`, `docs/landing/index.html`, NiceGUI `app.py`. 리디자인 요청이 없으면 이 톤을 따른다.
- 카피는 사용자가 할 일만. 운영 은어(`install_success`, `feedback_submit` 등)와 Gatekeeper 우회 안내는 넣지 않는다.
- CTA는 실제로 받을 수 있는 산출물과 모순되지 않게 쓴다.
- 데스크톱과 390px, 보이는 포커스, 빈 상태·구체적 에러를 검증한다. UI를 바꿨으면 브라우저로 흐름을 확인한다.
- 이슈를 새로 쓸 때는 스킬의 `[P0|P1|P2]` 본문 형식을 쓴다. 같은 원인으로 이슈를 쪼개지 않는다.
